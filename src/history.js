import { createHashHistory } from "history";
// import { URL_PATH_BASENAME } from "./api/path";
// { basename: URL_PATH_BASENAME }
const history = createHashHistory();
// history.listen((location, action) => {});

export default history;
